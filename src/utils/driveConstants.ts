// Google Drive "root" folder id — the Drive API uses the literal 'root' as
// the sentinel for the authenticated user's My Drive root. Mirrored in UI
// files (App.tsx, FolderSelectionScreen.tsx) that cannot import TS utils.
export const ROOT_FOLDER_ID = 'root';
