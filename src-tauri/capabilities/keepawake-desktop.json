{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "keepawake-desktop",
  "description": "Keepawake permission for the main window (plugin has no Android backend — keepawake 0.5.1 sys/mod.rs:14 compile_error)",
  "windows": ["main"],
  "platforms": ["windows", "linux", "macOS", "iOS"],
  "permissions": ["keepawake:allow-start", "keepawake:allow-stop"]
}
